# The corpus is somebody else's work, fetched on demand. See tools/fetch-corpus.py.
corpus/

# Build output
dist/
bin/
obj/
*.user

# Conformance runs are per-machine until they are promoted into support/
conformance/out/

# Comparison runs: gigabytes of frames, and a score that is only meaningful beside the engine
# version and machine that produced it. Quote baseline.json in a commit message, do not commit it.
harness/out/

# A run you can look at: index.html plus every block's worst frame, written by --gallery. Same
# reasoning as harness/out - gigabytes of frames, and a score only meaningful beside the engine
# version that produced it.
review/
